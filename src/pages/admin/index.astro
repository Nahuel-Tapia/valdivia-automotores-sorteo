---
import Layout from "@/layouts/Layout.astro"
import { ShieldCheck, DollarSign, Ticket, Lock, LogOut, Download, Search, CheckCircle, Mail, Eye, X, Send, RotateCcw, Calendar } from "lucide-astro"
---

<Layout title="Panel de Administración | Valdivia Automotores">
  <section class="bg-navy py-8 text-white">
    <div class="container-app flex items-center justify-between">
      <div>
        <span class="text-xs font-semibold uppercase tracking-widest text-brand-light">Dashboard Privado</span>
        <h1 class="mt-1 font-display text-2xl font-800 sm:text-3xl">Administración de Sorteo</h1>
      </div>
      <div class="flex items-center gap-3">
        <button id="btn-edit-count" class="hidden btn-secondary text-xs py-2 px-3 bg-brand/20 border-brand/30 text-white hover:bg-brand">
          <Ticket class="h-4 w-4 text-brand-light" />
          Total Boletos
        </button>
        <button id="btn-edit-date" class="hidden btn-secondary text-xs py-2 px-3 bg-brand/20 border-brand/30 text-white hover:bg-brand">
          <Calendar class="h-4 w-4 text-brand-light" />
          Fecha Sorteo
        </button>
        <button id="btn-edit-price" class="hidden btn-secondary text-xs py-2 px-3 bg-brand/20 border-brand/30 text-white hover:bg-brand">
          <DollarSign class="h-4 w-4 text-brand-light" />
          Precio Boleto
        </button>
        <button id="btn-reset-db" class="hidden btn-secondary text-xs py-2 px-3 bg-red-600/90 border-red-500 text-white hover:bg-red-700">
          <RotateCcw class="h-4 w-4" />
          Resetear DB
        </button>
        <button id="btn-export-csv" class="hidden btn-primary text-xs py-2 px-3 bg-brand text-white">
          <Download class="h-4 w-4" />
          Exportar CSV
        </button>
        <button id="btn-logout" class="hidden btn-secondary text-xs py-2 px-3 bg-white/10 border-white/20 text-white hover:bg-white/20">
          <LogOut class="h-4 w-4" />
          Cerrar Sesión
        </button>
      </div>
    </div>
  </section>

  <!-- Login Form -->
  <div id="login-section" class="container-app py-16">
    <div class="mx-auto max-w-md card p-8">
      <div class="flex items-center gap-3 border-b border-navy/10 pb-4">
        <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-brand/10 text-brand">
          <Lock class="h-5 w-5" />
        </span>
        <div>
          <h2 class="font-display text-lg font-700 text-navy">Acceso Administrador</h2>
          <p class="text-xs text-navy/60">Ingresá tus credenciales oficiales</p>
        </div>
      </div>

      <form id="admin-login-form" class="mt-6 space-y-4">
        <label class="block">
          <span class="text-xs font-700 text-navy/70 uppercase">Email</span>
          <input
            required
            type="email"
            id="admin-email"
            class="mt-1 h-11 w-full rounded-xl border border-navy/15 px-3 text-sm text-navy focus:border-brand focus:outline-none"
            placeholder="valdiviasorteo@admin.com"
          />
        </label>
        <label class="block">
          <span class="text-xs font-700 text-navy/70 uppercase">Contraseña</span>
          <input
            required
            type="password"
            id="admin-password"
            class="mt-1 h-11 w-full rounded-xl border border-navy/15 px-3 text-sm text-navy focus:border-brand focus:outline-none"
            placeholder="••••••••"
          />
        </label>
        <button type="submit" id="btn-login" class="btn-primary mt-2 w-full">
          Ingresar al Panel
        </button>
      </form>
    </div>
  </div>

  <!-- Dashboard Content -->
  <div id="dashboard-section" class="container-app hidden py-12 space-y-8">
    <!-- Tarjetas de Métricas -->
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      <div class="card p-5">
        <div class="flex items-center justify-between">
          <span class="text-xs font-700 uppercase tracking-wider text-navy/50">Recaudación Total</span>
          <span class="flex h-9 w-9 items-center justify-center rounded-lg bg-brand/10 text-brand">
            <DollarSign class="h-5 w-5" />
          </span>
        </div>
        <p class="mt-3 font-display text-2xl font-800 text-navy" id="m-revenue">$0</p>
        <p class="mt-1 text-xs text-navy/60"><span id="m-approved-orders">0</span> órdenes aprobadas</p>
      </div>

      <div class="card p-5">
        <div class="flex items-center justify-between">
          <span class="text-xs font-700 uppercase tracking-wider text-navy/50">Boletos Vendidos</span>
          <span class="flex h-9 w-9 items-center justify-center rounded-lg bg-green-500/10 text-green-600">
            <Ticket class="h-5 w-5" />
          </span>
        </div>
        <p class="mt-3 font-display text-2xl font-800 text-navy" id="m-paid">0</p>
        <p class="mt-1 text-xs text-navy/60">Confirmados en DB</p>
      </div>

      <div class="card p-5">
        <div class="flex items-center justify-between">
          <span class="text-xs font-700 uppercase tracking-wider text-navy/50">Boletos Reservados</span>
          <span class="flex h-9 w-9 items-center justify-center rounded-lg bg-amber-500/10 text-amber-600">
            <Ticket class="h-5 w-5" />
          </span>
        </div>
        <p class="mt-3 font-display text-2xl font-800 text-navy" id="m-reserved">0</p>
        <p class="mt-1 text-xs text-navy/60">Tiempo en ventana de 15m</p>
      </div>

      <div class="card p-5">
        <div class="flex items-center justify-between">
          <span class="text-xs font-700 uppercase tracking-wider text-navy/50">Boletos Disponibles</span>
          <span class="flex h-9 w-9 items-center justify-center rounded-lg bg-blue-500/10 text-blue-600">
            <Ticket class="h-5 w-5" />
          </span>
        </div>
        <p class="mt-3 font-display text-2xl font-800 text-navy" id="m-available">200</p>
        <p class="mt-1 text-xs text-navy/60">De <span id="m-total-tickets">200</span> totales</p>
      </div>
    </div>

    <!-- Tabla de Últimas Órdenes y Buscador -->
    <div class="card p-6">
      <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between mb-4">
        <div>
          <h2 class="font-display text-lg font-700 text-navy">Gestión de Órdenes y Participantes</h2>
          <p class="text-xs text-navy/60">Tocá cualquier orden para ver los números asignados o re-enviar comprobantes</p>
        </div>
        <div class="relative w-full sm:w-64">
          <Search class="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-navy/40" />
          <input
            type="text"
            id="search-orders"
            placeholder="Buscar por DNI, Nombre o ID..."
            class="h-9 w-full rounded-xl border border-navy/15 pl-9 pr-3 text-xs text-navy focus:border-brand focus:outline-none"
          />
        </div>
      </div>

      <div class="overflow-x-auto">
        <table class="w-full text-left text-sm">
          <thead class="border-b border-navy/10 text-xs font-700 uppercase text-navy/50">
            <tr>
              <th class="pb-3">ID Orden</th>
              <th class="pb-3">Comprador</th>
              <th class="pb-3">Email</th>
              <th class="pb-3">Boletos</th>
              <th class="pb-3">Monto</th>
              <th class="pb-3">Estado</th>
              <th class="pb-3 text-right">Acciones</th>
            </tr>
          </thead>
          <tbody id="orders-table-body" class="divide-y divide-navy/5 font-500">
            <tr>
              <td colspan="7" class="py-4 text-center text-navy/50">Cargando órdenes...</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <!-- Modal de Detalle Completo de Orden -->
  <div id="order-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-navy/60 backdrop-blur-sm p-4">
    <div class="relative w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl space-y-6">
      <div class="flex items-center justify-between border-b border-navy/10 pb-4">
        <div>
          <span class="text-xs font-700 uppercase text-brand">Ficha del Participante</span>
          <h3 id="modal-order-id" class="font-display text-xl font-800 text-navy">Orden #ORD-000000</h3>
        </div>
        <button type="button" id="modal-close-btn" class="rounded-lg p-1.5 text-navy/40 hover:bg-cloud hover:text-navy">
          <X class="h-5 w-5" />
        </button>
      </div>

      <!-- Datos del Comprador -->
      <div class="grid grid-cols-2 gap-4 text-xs">
        <div class="rounded-xl bg-cloud/60 p-3">
          <span class="text-navy/50 font-600 block">Nombre del Comprador</span>
          <strong id="modal-buyer-name" class="text-sm font-700 text-navy">--</strong>
        </div>
        <div class="rounded-xl bg-cloud/60 p-3">
          <span class="text-navy/50 font-600 block">DNI / Documento</span>
          <strong id="modal-buyer-dni" class="text-sm font-700 text-navy">--</strong>
        </div>
        <div class="rounded-xl bg-cloud/60 p-3">
          <span class="text-navy/50 font-600 block">Correo Electrónico</span>
          <strong id="modal-buyer-email" class="text-sm font-700 text-navy">--</strong>
        </div>
        <div class="rounded-xl bg-cloud/60 p-3">
          <span class="text-navy/50 font-600 block">Teléfono / WhatsApp</span>
          <strong id="modal-buyer-phone" class="text-sm font-700 text-navy">--</strong>
        </div>
      </div>

      <!-- Estado y Pago -->
      <div class="flex items-center justify-between border-t border-b border-navy/10 py-3 text-xs">
        <div>
          <span class="text-navy/50 font-600">Medio de Pago:</span>
          <strong id="modal-payment-method" class="ml-1 uppercase text-navy font-700">--</strong>
        </div>
        <div>
          <span class="text-navy/50 font-600">Monto Total:</span>
          <strong id="modal-total-amount" class="ml-1 text-sm font-800 text-brand">--</strong>
        </div>
        <div id="modal-status-badge">
          <!-- Badge dinámico -->
        </div>
      </div>

      <!-- Grilla de Números de la Suerte Asignados -->
      <div>
        <h4 class="font-display text-xs font-700 uppercase tracking-wider text-navy/60 mb-2">Boletos de la Suerte Asignados</h4>
        <div id="modal-tickets-grid" class="flex flex-wrap gap-2 max-h-40 overflow-y-auto p-2 rounded-xl bg-cloud/50 border border-navy/10">
          <span class="text-xs text-navy/40">Cargando boletos...</span>
        </div>
      </div>

      <!-- Acciones de Gestión -->
      <div class="flex flex-wrap items-center justify-between gap-3 pt-2">
        <button
          type="button"
          id="modal-resend-email-btn"
          class="btn-secondary text-xs py-2.5 px-4 bg-brand/10 border-brand/20 text-brand hover:bg-brand hover:text-white transition font-700"
        >
          <Send class="h-4 w-4 inline mr-1" />
          Re-enviar Comprobante por Email
        </button>

        <div id="modal-action-approve-container">
          <!-- Botón aprobar si está pending -->
        </div>
      </div>
    </div>
  </div>

  <!-- Modal para Modificar Precio del Boleto -->
  <div id="price-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-navy/60 backdrop-blur-sm p-4">
    <div class="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl space-y-5">
      <div class="flex items-center justify-between border-b border-navy/10 pb-3">
        <div class="flex items-center gap-2.5">
          <span class="flex h-9 w-9 items-center justify-center rounded-xl bg-brand/10 text-brand">
            <DollarSign class="h-5 w-5" />
          </span>
          <div>
            <h3 class="font-display text-base font-800 text-navy">Editar Precio por Boleto</h3>
            <p class="text-xs text-navy/60">Configuración global del sorteo</p>
          </div>
        </div>
        <button type="button" id="price-modal-close-btn" class="rounded-lg p-1.5 text-navy/40 hover:bg-cloud hover:text-navy">
          <X class="h-5 w-5" />
        </button>
      </div>

      <form id="form-update-price" class="space-y-4">
        <div>
          <label class="block text-xs font-700 text-navy/70 uppercase mb-1">Nuevo Valor Individual ($)</label>
          <div class="relative">
            <span class="absolute left-3 top-1/2 -translate-y-1/2 text-sm font-700 text-navy/50">$</span>
            <input 
              type="number" 
              id="input-ticket-price" 
              min="1" 
              step="1" 
              required
              class="h-11 w-full rounded-xl border border-navy/15 pl-8 pr-3 text-base font-700 text-navy focus:border-brand focus:outline-none"
              placeholder="20"
            />
          </div>
          <p class="mt-2 text-xs text-navy/50 leading-relaxed">
            Se aplicará inmediatamente en el cotizador de boletos de la web y en las preferencias de cobro de Mercado Pago.
          </p>
        </div>

        <div class="flex items-center justify-end gap-3 pt-2">
          <button type="button" id="price-modal-cancel-btn" class="btn-secondary text-xs py-2.5 px-4">
            Cancelar
          </button>
          <button type="submit" id="btn-save-price" class="btn-primary text-xs py-2.5 px-4">
            Guardar Cambios
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Modal para Modificar Fecha del Sorteo -->
  <div id="date-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-navy/60 backdrop-blur-sm p-4">
    <div class="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl space-y-5">
      <div class="flex items-center justify-between border-b border-navy/10 pb-3">
        <div class="flex items-center gap-2.5">
          <span class="flex h-9 w-9 items-center justify-center rounded-xl bg-brand/10 text-brand">
            <Calendar class="h-5 w-5" />
          </span>
          <div>
            <h3 class="font-display text-base font-800 text-navy">Editar Fecha del Sorteo</h3>
            <p class="text-xs text-navy/60">Sincronización del contador de la web</p>
          </div>
        </div>
        <button type="button" id="date-modal-close-btn" class="rounded-lg p-1.5 text-navy/40 hover:bg-cloud hover:text-navy">
          <X class="h-5 w-5" />
        </button>
      </div>

      <form id="form-update-date" class="space-y-4">
        <div>
          <label class="block text-xs font-700 text-navy/70 uppercase mb-1">Fecha y Hora del Sorteo</label>
          <input 
            type="datetime-local" 
            id="input-draw-date" 
            required
            class="h-11 w-full rounded-xl border border-navy/15 px-3 text-sm font-600 text-navy focus:border-brand focus:outline-none"
          />
          <p class="mt-2 text-xs text-navy/50 leading-relaxed">
            El reloj de la portada principal (contador en vivo) y las bases y condiciones se actualizarán automáticamente a la nueva fecha ingresada.
          </p>
        </div>

        <div class="flex items-center justify-end gap-3 pt-2">
          <button type="button" id="date-modal-cancel-btn" class="btn-secondary text-xs py-2.5 px-4">
            Cancelar
          </button>
          <button type="submit" id="btn-save-date" class="btn-primary text-xs py-2.5 px-4">
            Guardar Fecha
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Modal para Modificar Cantidad Total de Boletos -->
  <div id="count-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-navy/60 backdrop-blur-sm p-4">
    <div class="relative w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl space-y-5">
      <div class="flex items-center justify-between border-b border-navy/10 pb-3">
        <div class="flex items-center gap-2.5">
          <span class="flex h-9 w-9 items-center justify-center rounded-xl bg-brand/10 text-brand">
            <Ticket class="h-5 w-5" />
          </span>
          <div>
            <h3 class="font-display text-base font-800 text-navy">Editar Total de Boletos a la Venta</h3>
            <p class="text-xs text-navy/60">Ampliación o ajuste de inventario</p>
          </div>
        </div>
        <button type="button" id="count-modal-close-btn" class="rounded-lg p-1.5 text-navy/40 hover:bg-cloud hover:text-navy">
          <X class="h-5 w-5" />
        </button>
      </div>

      <form id="form-update-count" class="space-y-4">
        <div>
          <label class="block text-xs font-700 text-navy/70 uppercase mb-1">Cantidad Total de Números</label>
          <input 
            type="number" 
            id="input-ticket-count" 
            min="1"
            max="9999"
            step="1"
            required
            class="h-11 w-full rounded-xl border border-navy/15 px-3 text-base font-700 text-navy focus:border-brand focus:outline-none"
            placeholder="200"
          />
          <p class="mt-2 text-xs text-navy/50 leading-relaxed">
            Al aumentar (ej: de 200 a 500), la grilla generará automáticamente los nuevos boletos disponibles en tiempo real.
          </p>
        </div>

        <div class="flex items-center justify-end gap-3 pt-2">
          <button type="button" id="count-modal-cancel-btn" class="btn-secondary text-xs py-2.5 px-4">
            Cancelar
          </button>
          <button type="submit" id="btn-save-count" class="btn-primary text-xs py-2.5 px-4">
            Guardar Cantidad
          </button>
        </div>
      </form>
    </div>
  </div>
</Layout>

<script>
  import { formatARS } from "@/lib/cart"

  function initAdmin() {
    const loginSec = document.getElementById("login-section")
    const dashSec = document.getElementById("dashboard-section")
    const form = document.getElementById("admin-login-form") as HTMLFormElement | null
    const logoutBtn = document.getElementById("btn-logout")
    const exportBtn = document.getElementById("btn-export-csv")
    const searchInput = document.getElementById("search-orders") as HTMLInputElement | null
    const resetDbBtn = document.getElementById("btn-reset-db")
    const editPriceBtn = document.getElementById("btn-edit-price")
    const priceModal = document.getElementById("price-modal")
    const priceModalCloseBtn = document.getElementById("price-modal-close-btn")
    const priceModalCancelBtn = document.getElementById("price-modal-cancel-btn")

    const editDateBtn = document.getElementById("btn-edit-date")
    const dateModal = document.getElementById("date-modal")
    const dateModalCloseBtn = document.getElementById("date-modal-close-btn")
    const dateModalCancelBtn = document.getElementById("date-modal-cancel-btn")

    const editCountBtn = document.getElementById("btn-edit-count")
    const countModal = document.getElementById("count-modal")
    const countModalCloseBtn = document.getElementById("count-modal-close-btn")
    const countModalCancelBtn = document.getElementById("count-modal-cancel-btn")

    // Elementos del Modal
    const orderModal = document.getElementById("order-modal")
    const modalCloseBtn = document.getElementById("modal-close-btn")
    const resendEmailBtn = document.getElementById("modal-resend-email-btn")
    let currentSelectedOrderId: string | null = null

    let allOrders: any[] = []

    function getAdminToken(): string | null {
      return sessionStorage.getItem("valdivia_admin_token") || localStorage.getItem("valdivia_admin_token")
    }

    function handleSessionExpired() {
      sessionStorage.removeItem("valdivia_admin_token")
      localStorage.removeItem("valdivia_admin_token")
      loginSec?.classList.remove("hidden")
      dashSec?.classList.add("hidden")
      logoutBtn?.classList.add("hidden")
      exportBtn?.classList.add("hidden")
      resetDbBtn?.classList.add("hidden")
      editPriceBtn?.classList.add("hidden")
      editDateBtn?.classList.add("hidden")
      editCountBtn?.classList.add("hidden")
      orderModal?.classList.add("hidden")
      orderModal?.classList.remove("flex")
      priceModal?.classList.add("hidden")
      priceModal?.classList.remove("flex")
      dateModal?.classList.add("hidden")
      dateModal?.classList.remove("flex")
      countModal?.classList.add("hidden")
      countModal?.classList.remove("flex")
    }

    // Siempre solicitar credenciales de inicio de sesión al ingresar/recargar la pestaña /admin
    handleSessionExpired()

    form?.addEventListener("submit", async (e) => {
      e.preventDefault()
      const email = (document.getElementById("admin-email") as HTMLInputElement).value
      const password = (document.getElementById("admin-password") as HTMLInputElement).value

      try {
        const res = await fetch("/api/admin/login", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email, password }),
        })
        const data = await res.json()
        if (data.success && data.token) {
          sessionStorage.setItem("valdivia_admin_token", data.token)
          showDashboard()
        } else {
          alert(data.error || "Credenciales incorrectas")
        }
      } catch (err) {
        alert("Error de conexión al autenticar")
      }
    })

    logoutBtn?.addEventListener("click", () => {
      handleSessionExpired()
    })

    exportBtn?.addEventListener("click", async () => {
      const currentToken = getAdminToken()
      try {
        const res = await fetch("/api/admin/export", {
          headers: { Authorization: `Bearer ${currentToken}` },
        })
        if (res.status === 401 || res.status === 403) {
          alert("🔒 Sesión expirada. Por favor volvaz a ingresar.")
          handleSessionExpired()
          return
        }
        const blob = await res.blob()
        const url = window.URL.createObjectURL(blob)
        const a = document.createElement("a")
        a.href = url
        a.download = `lista_oficial_participantes_${Date.now()}.csv`
        document.body.appendChild(a)
        a.click()
        a.remove()
        window.URL.revokeObjectURL(url)
      } catch {
        alert("Error de conexión al exportar CSV.")
      }
    })

    searchInput?.addEventListener("input", (e) => {
      const q = (e.target as HTMLInputElement).value.trim().toLowerCase()
      renderOrdersTable(allOrders.filter(o => 
        o.id.toLowerCase().includes(q) || 
        o.buyerName.toLowerCase().includes(q) || 
        o.buyerEmail.toLowerCase().includes(q)
      ))
    })

    modalCloseBtn?.addEventListener("click", () => {
      orderModal?.classList.add("hidden")
      orderModal?.classList.remove("flex")
    })

    resendEmailBtn?.addEventListener("click", async () => {
      if (!currentSelectedOrderId) return
      const currentToken = getAdminToken()
      resendEmailBtn.setAttribute("disabled", "true")
      resendEmailBtn.textContent = "Enviando correo..."

      try {
        const res = await fetch("/api/admin/resend-email", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${currentToken}`,
          },
          body: JSON.stringify({ orderId: currentSelectedOrderId }),
        })
        const data = await res.json()
        if (data.success) {
          alert(`✅ ${data.message}`)
        } else {
          alert(`❌ ${data.error || "No se pudo enviar el correo"}`)
        }
      } catch {
        alert("Error de conexión al re-enviar correo.")
      } finally {
        resendEmailBtn.removeAttribute("disabled")
        resendEmailBtn.innerHTML = `<svg class="h-4 w-4 inline mr-1" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg> Re-enviar Comprobante por Email`
      }
    })

    resetDbBtn?.addEventListener("click", async () => {
      if (!confirm("⚠️ ¿Estás seguro de que deseas resetear por completo la base de datos?\n\nSe borrarán todas las órdenes y se restablecerán los 200 boletos a estado disponible.")) {
        return
      }

      const currentToken = getAdminToken()
      try {
        const res = await fetch("/api/admin/reset-db", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${currentToken}`,
          },
        })

        if (res.status === 401 || res.status === 403) {
          alert("🔒 Tu sesión de administrador ha expirado. Por favor, ingresa nuevamente.")
          handleSessionExpired()
          return
        }

        const data = await res.json()
        if (data.success) {
          alert(`✨ ${data.message}`)
          showDashboard()
        } else {
          alert(`❌ ${data.error || "No se pudo resetear la base de datos."}`)
        }
      } catch {
        alert("Error de conexión al intentar resetear la base de datos.")
      }
    })

    editPriceBtn?.addEventListener("click", async () => {
      await loadSettings()
      priceModal?.classList.remove("hidden")
      priceModal?.classList.add("flex")
    })

    const closePriceModal = () => {
      priceModal?.classList.add("hidden")
      priceModal?.classList.remove("flex")
    }

    priceModalCloseBtn?.addEventListener("click", closePriceModal)
    priceModalCancelBtn?.addEventListener("click", closePriceModal)

    editDateBtn?.addEventListener("click", async () => {
      await loadSettings()
      dateModal?.classList.remove("hidden")
      dateModal?.classList.add("flex")
    })

    const closeDateModal = () => {
      dateModal?.classList.add("hidden")
      dateModal?.classList.remove("flex")
    }

    dateModalCloseBtn?.addEventListener("click", closeDateModal)
    dateModalCancelBtn?.addEventListener("click", closeDateModal)

    editCountBtn?.addEventListener("click", async () => {
      await loadSettings()
      countModal?.classList.remove("hidden")
      countModal?.classList.add("flex")
    })

    const closeCountModal = () => {
      countModal?.classList.add("hidden")
      countModal?.classList.remove("flex")
    }

    countModalCloseBtn?.addEventListener("click", closeCountModal)
    countModalCancelBtn?.addEventListener("click", closeCountModal)

    async function showDashboard() {
      const currentToken = getAdminToken()
      if (!currentToken) {
        handleSessionExpired()
        return
      }

      loginSec?.classList.add("hidden")
      dashSec?.classList.remove("hidden")
      editPriceBtn?.classList.remove("hidden")
      editDateBtn?.classList.remove("hidden")
      editCountBtn?.classList.remove("hidden")
      logoutBtn?.classList.remove("hidden")
      exportBtn?.classList.remove("hidden")
      resetDbBtn?.classList.remove("hidden")

      loadSettings()

      try {
        const res = await fetch("/api/admin/metrics", {
          headers: { Authorization: `Bearer ${currentToken}` },
        })
        if (res.status === 401 || res.status === 403) {
          alert("🔒 Tu sesión ha expirado. Por favor ingresá nuevamente.")
          handleSessionExpired()
          return
        }
        const data = await res.json()
        if (data.success) {
          const m = data.metrics
          document.getElementById("m-revenue")!.textContent = formatARS(m.totalRevenue)
          document.getElementById("m-approved-orders")!.textContent = String(m.approvedOrders)
          document.getElementById("m-paid")!.textContent = String(m.paidTickets)
          document.getElementById("m-reserved")!.textContent = String(m.reservedTickets)
          document.getElementById("m-available")!.textContent = String(m.availableTickets)
          const mTotal = document.getElementById("m-total-tickets")
          if (mTotal) mTotal.textContent = String(m.totalTickets)

          allOrders = data.recentOrders || []
          renderOrdersTable(allOrders)
        }
      } catch (err) {
        console.error("Error al cargar métricas de admin:", err)
      }
    }

    async function loadSettings() {
      const currentToken = getAdminToken()
      try {
        const res = await fetch("/api/admin/settings", {
          headers: { Authorization: `Bearer ${currentToken}` },
        })
        const data = await res.json()
        if (data.success) {
          if (data.ticketPrice) {
            const inputPrice = document.getElementById("input-ticket-price") as HTMLInputElement | null
            if (inputPrice) inputPrice.value = String(data.ticketPrice)
          }
          if (data.drawDate) {
            const inputDate = document.getElementById("input-draw-date") as HTMLInputElement | null
            if (inputDate) {
              const d = new Date(data.drawDate)
              if (!isNaN(d.getTime())) {
                const pad = (n: number) => String(n).padStart(2, "0")
                inputDate.value = `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`
              }
            }
          }
          if (data.totalTickets) {
            const inputCount = document.getElementById("input-ticket-count") as HTMLInputElement | null
            if (inputCount) inputCount.value = String(data.totalTickets)
          }
        }
      } catch (err) {
        console.error("Error cargando configuraciones de admin:", err)
      }
    }

    const priceForm = document.getElementById("form-update-price") as HTMLFormElement | null
    priceForm?.addEventListener("submit", async (e) => {
      e.preventDefault()
      const currentToken = getAdminToken()
      const priceInput = document.getElementById("input-ticket-price") as HTMLInputElement | null
      const newPrice = Number(priceInput?.value)

      if (isNaN(newPrice) || newPrice <= 0) {
        alert("Por favor ingresá un precio válido mayor a 0.")
        return
      }

      try {
        const res = await fetch("/api/admin/settings", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${currentToken}`,
          },
          body: JSON.stringify({ ticketPrice: newPrice }),
        })
        const data = await res.json()
        if (data.success) {
          alert(`✨ ${data.message}`)
          closePriceModal()
          showDashboard()
        } else {
          alert(`❌ ${data.error || "No se pudo actualizar el precio."}`)
        }
      } catch {
        alert("Error de conexión al actualizar el precio.")
      }
    })

    const dateForm = document.getElementById("form-update-date") as HTMLFormElement | null
    dateForm?.addEventListener("submit", async (e) => {
      e.preventDefault()
      const currentToken = getAdminToken()
      const dateInput = document.getElementById("input-draw-date") as HTMLInputElement | null
      const rawVal = dateInput?.value

      if (!rawVal) {
        alert("Por favor elegí una fecha y hora válidas.")
        return
      }

      const isoDate = new Date(rawVal).toISOString()

      try {
        const res = await fetch("/api/admin/settings", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${currentToken}`,
          },
          body: JSON.stringify({ drawDate: isoDate }),
        })
        const data = await res.json()
        if (data.success) {
          alert(`✨ ${data.message}`)
          closeDateModal()
          showDashboard()
        } else {
          alert(`❌ ${data.error || "No se pudo actualizar la fecha del sorteo."}`)
        }
      } catch {
        alert("Error de conexión al actualizar la fecha del sorteo.")
      }
    })

    const countForm = document.getElementById("form-update-count") as HTMLFormElement | null
    countForm?.addEventListener("submit", async (e) => {
      e.preventDefault()
      const currentToken = getAdminToken()
      const countInput = document.getElementById("input-ticket-count") as HTMLInputElement | null
      const newCount = Number(countInput?.value)

      if (isNaN(newCount) || newCount <= 0) {
        alert("Por favor ingresá una cantidad de boletos válida mayor a 0.")
        return
      }

      try {
        const res = await fetch("/api/admin/settings", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${currentToken}`,
          },
          body: JSON.stringify({ totalTickets: newCount }),
        })
        const data = await res.json()
        if (data.success) {
          alert(`✨ ${data.message}`)
          closeCountModal()
          showDashboard()
        } else {
          alert(`❌ ${data.error || "No se pudo actualizar la cantidad de boletos."}`)
        }
      } catch {
        alert("Error de conexión al actualizar la cantidad de boletos.")
      }
    })

    function renderOrdersTable(orders: any[]) {
      const tbody = document.getElementById("orders-table-body")
      if (!tbody) return

      if (orders.length === 0) {
        tbody.innerHTML = `<tr><td colspan="7" class="py-4 text-center text-navy/50">No hay órdenes registradas aún.</td></tr>`
        return
      }

      tbody.innerHTML = orders
        .map(
          (o: any) => `
        <tr class="hover:bg-cloud/70 cursor-pointer group transition">
          <td class="py-3 font-700 text-brand btn-view-detail" data-order="${o.id}">${o.id}</td>
          <td class="py-3 font-600 text-navy btn-view-detail" data-order="${o.id}">${o.buyerName}</td>
          <td class="py-3 text-navy/70 btn-view-detail" data-order="${o.id}">${o.buyerEmail}</td>
          <td class="py-3 font-700 text-navy btn-view-detail" data-order="${o.id}">${o.ticketCount}</td>
          <td class="py-3 font-700 text-navy btn-view-detail" data-order="${o.id}">${formatARS(o.totalAmount)}</td>
          <td class="py-3 btn-view-detail" data-order="${o.id}">
            <span class="rounded-full px-2.5 py-1 text-xs font-800 ${
              o.status === "approved"
                ? "bg-green-100 text-green-700"
                : o.status === "pending"
                ? "bg-amber-100 text-amber-700"
                : "bg-red-100 text-red-700"
            }">
              ${o.status.toUpperCase()}
            </span>
          </td>
          <td class="py-3 text-right">
            <button data-order="${o.id}" class="btn-view-detail rounded-lg bg-navy/10 px-2.5 py-1 text-xs font-700 text-navy hover:bg-navy hover:text-white transition">
              Ver Detalle
            </button>
          </td>
        </tr>
      `
        )
        .join("")

      // Handlers de Detalle
      tbody.querySelectorAll(".btn-view-detail").forEach((el) => {
        el.addEventListener("click", () => {
          const orderId = (el as HTMLElement).dataset.order
          if (orderId) openOrderDetailModal(orderId)
        })
      })
    }

    async function openOrderDetailModal(orderId: string) {
      const currentToken = getAdminToken()
      currentSelectedOrderId = orderId
      orderModal?.classList.remove("hidden")
      orderModal?.classList.add("flex")

      document.getElementById("modal-order-id")!.textContent = `Orden #${orderId}`
      document.getElementById("modal-tickets-grid")!.innerHTML = `<span class="text-xs text-navy/40">Cargando boletos...</span>`

      try {
        const res = await fetch(`/api/admin/order-details?orderId=${orderId}`, {
          headers: { Authorization: `Bearer ${currentToken}` },
        })
        const data = await res.json()

        if (!data.success) {
          alert(data.error || "No se pudo consultar el detalle de la orden.")
          return
        }

        const o = data.order
        document.getElementById("modal-buyer-name")!.textContent = o.buyerName
        document.getElementById("modal-buyer-dni")!.textContent = o.buyerDni
        document.getElementById("modal-buyer-email")!.textContent = o.buyerEmail
        document.getElementById("modal-buyer-phone")!.textContent = o.buyerPhone
        document.getElementById("modal-payment-method")!.textContent = o.paymentMethod
        document.getElementById("modal-total-amount")!.textContent = formatARS(o.totalAmount)

        const badgeContainer = document.getElementById("modal-status-badge")!
        badgeContainer.innerHTML = `
          <span class="rounded-full px-3 py-1 text-xs font-800 ${
            o.status === "approved"
              ? "bg-green-100 text-green-700"
              : o.status === "pending"
              ? "bg-amber-100 text-amber-700"
              : "bg-red-100 text-red-700"
          }">
            ${o.status.toUpperCase()}
          </span>
        `

        // Grid de boletos
        const ticketsGrid = document.getElementById("modal-tickets-grid")!
        if (data.tickets.length === 0) {
          ticketsGrid.innerHTML = `<span class="text-xs text-navy/40">Sin boletos asignados</span>`
        } else {
          ticketsGrid.innerHTML = data.tickets
            .map(
              (t: any) => `
            <span class="rounded-lg bg-navy text-gold px-2.5 py-1 font-display text-xs font-800 shadow-sm border border-navy/20">
              #${t.number}
            </span>
          `
            )
            .join("")
        }

      } catch (err) {
        console.error("Error abriendo modal:", err)
      }
    }
  }

  initAdmin()
  document.addEventListener("astro:page-load", initAdmin)
</script>
